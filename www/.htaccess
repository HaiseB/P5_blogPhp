# The serveur must follow the symbolicals links :
Options +FollowSymlinks

# Enable the URL rewriting mod :
RewriteEngine on

#--------------------------------------------------
# Rules :
#--------------------------------------------------

# Main :
RewriteRule ^(.*).html$ index.php?p=$1 [L]